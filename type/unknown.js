"use strict";
// any와 짝으로 any 보다 편하
const aNum = maybe;
if (maybe === true) {
    const aBoolean = maybe;
    // const aString:string=maybe
}
if (typeof maybe === 'string') {
    const aString = maybe;
}
